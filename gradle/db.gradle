// Migration tasks, properties are environment dependent.
task migrate {
  // supports only one profile.
  doLast {
    println "Executing $propertyDrivenProfile migrate task actions for changelog file. "
      liquibase {
        activities {
          main {
            // changeLogFile "$projectDir$liquibase.changelog"
              // url          "$datasource_url"
              username      "$ext.spring.datasource.username"
              // password      "$datasource_password"
          }
        }
      }
  }
}
